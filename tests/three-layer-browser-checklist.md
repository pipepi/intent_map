# Three-layer browser acceptance

- Open `/intent-editor`; verify empty element/type lists and no console errors.
- Install both samples; verify 2 element plugins, 2 type plugins, and 3 addable node types.
- Create text, image, and image-description nodes; edit every field and verify isolated previews.
- Exercise click, modifier multi-select, box selection, context menu, and type export.
- Export selection and full collection ZIPs, refresh, then import and verify data/positions/dependencies.
- Disable an element and uninstall a type; verify missing dependency/type placeholders.
- Import malformed, hash-mismatched, and missing-dependency files; verify state remains unchanged.
