# Intent Map 数字货币交易所核心业务流优化建议 v2

> 走查时间：2026-07-27  
> 走查方式：使用 Codex 内嵌浏览器对 `http://127.0.0.1:5500/index.html` 进行真实点击、下探、Surface 新增和校验联动测试，并结合 `app/runtime/sample-business-tree.ts` 核对数据骨架。  
> 目标场景：中心化数字货币交易所的基础现货交易能力。  
> 理念标尺：**一切皆管道（节点）**。内树表达稳定归属，外树表达工作视角，管道表达数据、命令、事件和约束如何流动。

---

## 一、结论摘要

当前 v3 工作区已经具备承载交易所业务建模的基础能力：

- 工作台、自由布局两个 Panel 可以同时投射同一棵内树。
- 节点树单击后，当前容器和属性上下文能够在同 Panel 内联动。
- 当前容器可以逐层下探，功能 Surface 可以动态加入。
- 校验器能够发现未映射输出、未消费环境输入和未消费节点输出。
- 浏览器走查期间未发现控制台错误。

但“核心业务流动场景与约束”仍是通用软件交付占位模型，不足以表达交易所：

1. 核心节点只有一个子节点“参与者与触发条件”，画布大面积空白。
2. 第四层“识别核心参与者”的描述仍是“四层初始化叶子意图”。
3. 产品目标、业务约束没有被子节点消费，参与者输出也没有继续流动。
4. 核心输出“场景序列与约束”没有映射。
5. 订单、撮合、资金、账本、清算、行情、风控和审计没有成为一等节点。
6. 当前数据流不足以表达交易所最关键的确定性、不变量、幂等与异常恢复。

因此，新版本应优先把 `scenario_flow` 从“需求分析占位容器”升级成“可执行、可校验、可追踪的现货交易业务管道”。

---

## 二、浏览器实测发现

### 1. 核心节点只有一条占位子链

- **路径**：工作台 → 意图结构树 → 单击“核心业务流动场景序列与约束”。
- **当前内容**：

```text
核心业务流动场景序列与约束
└─ 参与者与触发条件
   └─ 识别核心参与者
```

- **实测表现**：当前容器切换到核心节点后，画布只有一个子节点，55% 相机下大部分区域为空。
- **影响**：用户无法从画布理解一笔交易从账户、资金、订单、撮合到清算的完整流动。
- **建议**：先在当前默认样例的第三层并列展开交易所核心能力域，再从每个能力域继续递归下探可执行或可校验的业务步骤。四层只是当前样例展开到的深度，不是内树限制；任意节点都应能继续细分。

### 2. 现有端口和管道没有形成闭环

核心节点当前端口：

```text
输入：
- 产品目标（绑定环境输入）
- 业务约束（绑定环境输入）

输出：
- 场景序列与约束（未映射）
```

浏览器打开“静态校验”后，在核心节点作用域得到：

```text
0 错误 · 1 警告 · 3 提示
- 容器输出「场景序列与约束」未映射
- 环境输入「产品目标」未被任何节点消费
- 环境输入「业务约束」未被任何节点消费
- 「参与者与触发条件」的输出未被消费
```

这说明当前树满足“有节点”，但尚未满足“一切皆管道”：

- 输入没有进入子节点。
- 子节点输出没有进入后续步骤。
- 容器输出没有从内部结果映射出来。
- 校验已经能发现问题，但默认测试向量本身不是一个校验通过的示范。

### 3. 第四层叶节点缺少业务语义

- **节点**：`scenario_actor_leaf`
- **当前名称**：识别核心参与者
- **当前描述**：四层初始化叶子意图
- **当前结构**：0 输入、0 子节点、1 输出

建议保留 ID 和四层路径用于回归，但将内容升级为真实业务定义：

- 名称：识别交易参与方、权限与触发入口
- 输入：产品目标、业务约束、协作角色
- 输出：参与方与权限模型
- 内容：普通用户、做市商、运营、合规、风控、财务、托管/钱包、撮合系统、账本系统及外部链节点。

### 4. 新增功能 Surface 的默认落点干扰走查

- 新增“静态校验”后，Surface 默认出现在工作区中部偏左，与节点树/属性 Surface 和跨 Surface 连线发生视觉重叠。
- 属性编辑器从左下角展开后再次最小化，会缩到左上角，而不是回到默认左下角停靠位置。
- 多条跨 Surface 管道覆盖在左侧标题和内容附近，新增 Surface 越多越难追踪来源。

建议：

1. 最小化时保存独立的 `minimizedFrame`，展开时使用 `expandedFrame`，不要用当前展开位置反推最小化位置。
2. 新增 Surface 使用“空闲矩形查找”，优先填充未占用区域。
3. 新增后自动获得焦点，但不得遮住当前活动容器的核心内容。
4. 管道在 Surface 重叠时沿可见边界重新路由；被遮挡端点使用边界桩。

---

## 三、目标内树：数字货币现货交易所可递归骨架

下面先沿用默认样例现有四层路径展示第一版交易所骨架，以便复用既有 ID 和回归向量。这里的“第 1～4 层”只是当前展开结果，不构成最大深度约束；任何复合节点都可以继续增加子节点、递归下探到更细的业务步骤。

```text
第 1 层  business_root
数字货币现货交易所
│
├─ 第 2 层  scenario_flow
│  现货交易核心业务流与约束
│  │
│  ├─ 第 3 层  scenario_participants
│  │  参与方、权限与触发入口
│  │  └─ 第 4 层  scenario_actor_leaf
│  │     识别交易参与方、权限与入口
│  │
│  ├─ 第 3 层  account_access
│  │  账户准入与安全
│  │  └─ 第 4 层  account_access_check
│  │     KYC、状态、MFA、地区与制裁检查
│  │
│  ├─ 第 3 层  asset_funding
│  │  充值、资金可用性与冻结
│  │  └─ 第 4 层  balance_reservation
│  │     余额校验、冻结与幂等预占
│  │
│  ├─ 第 3 层  spot_order_lifecycle
│  │  现货订单生命周期
│  │  └─ 第 4 层  order_command
│  │     下单、撤单、过期与状态转换
│  │
│  ├─ 第 3 层  matching_execution
│  │  撮合与成交生成
│  │  └─ 第 4 层  price_time_match
│  │     价格优先、时间优先与成交序列
│  │
│  ├─ 第 3 层  clearing_settlement
│  │  清算、费用与双分录账本
│  │  └─ 第 4 层  ledger_posting
│  │     成交清算、手续费和余额入账
│  │
│  ├─ 第 3 层  market_data
│  │  行情与市场状态发布
│  │  └─ 第 4 层  market_projection
│  │     深度、Ticker、K 线与成交推送
│  │
│  └─ 第 3 层  risk_audit
│     风控、合规与审计证据
│     └─ 第 4 层  risk_audit_decision
│        限额、AML、熔断、追踪和对账
│
├─ 第 2 层  ui_consensus
│  交易终端与运营后台流程共识
├─ 第 2 层  database_schema
│  交易、订单、账本与审计数据模型
└─ 第 2 层  business_api
   交易 API、行情 API 与事件契约
```

### 保留与调整原则

- 保留 `scenario_flow → scenario_participants → scenario_actor_leaf` 的既有 ID 和父子关系，避免破坏固定测试向量。
- 第一版可先为每个第三层能力域增加一个第四层步骤，随后按业务需要继续向下拆分状态机、规则、异常、补偿和审计节点。
- `ui_consensus`、`database_schema`、`business_api` 仍保留为 `business_root` 的同级流程节点。
- 节点 position/size 属于默认投射几何，用户调整仍写入外树。
- 业务定义存于内树；Panel、Surface、相机和最小化位置不得进入业务节点。
- 内树深度不设上限；加载器和渲染器必须使用递归模型，不得把“四层”编码为校验、导航、布局或持久化限制。

---

## 四、核心节点端口与管道设计

### 1. `business_root` 建议端口

输入：

| 端口                   | 类型   | 说明                                   |
| ---------------------- | ------ | -------------------------------------- |
| `product_goal`         | object | 目标市场、用户群、交易品种、SLA        |
| `business_constraints` | object | 司法辖区、合规、资金与技术约束         |
| `stakeholders`         | array  | 用户、运营、合规、风控、财务、技术角色 |
| `market_catalog`       | array  | 交易对、精度、最小数量、价格步长       |
| `fee_policy`           | object | Maker/Taker、VIP、币种与舍入策略       |

输出：

| 端口                 | 类型   | 映射来源                         |
| -------------------- | ------ | -------------------------------- |
| `exchange_blueprint` | object | `business_api.exchange_contract` |
| `audit_acceptance`   | object | `scenario_flow.audit_ready_spec` |

### 2. `scenario_flow` 建议端口

输入：

| 端口             | 绑定                                      |
| ---------------- | ----------------------------------------- |
| `product_goal`   | 环境 `business_root.product_goal`         |
| `constraints`    | 环境 `business_root.business_constraints` |
| `stakeholders`   | 环境 `business_root.stakeholders`         |
| `market_catalog` | 环境 `business_root.market_catalog`       |
| `fee_policy`     | 环境 `business_root.fee_policy`           |

输出：

| 端口              | 类型   | 映射                                    |
| ----------------- | ------ | --------------------------------------- |
| `scenario_spec`   | object | `risk_audit.audit_ready_spec`           |
| `event_contracts` | array  | `market_data.market_events`             |
| `invariants`      | array  | `clearing_settlement.ledger_invariants` |

### 3. 主业务管道

```text
参与方与权限
      ↓ actor_context
账户准入与安全
      ↓ eligible_account
充值与资金可用性
      ↓ available_balance
订单生命周期
      ↓ accepted_order
撮合与成交
      ↓ trade_events
清算、费用与账本
      ├─ settlement_events → 行情投射
      └─ ledger_evidence  → 风控审计
                              ↓
                    可审计场景规格
```

旁路管道：

```text
业务约束 ─────────────→ 准入 / 下单 / 撮合 / 提现 / 风控
市场目录 ─────────────→ 下单校验 / 撮合 / 行情
费率策略 ─────────────→ 订单预估 / 清算 / API 展示
风险决策 ──拒绝/限流──→ 订单生命周期
所有命令与事件 ───────→ 审计追踪
```

### 4. 管道通道建议

- `data`：账户、余额、订单、成交和账本快照。
- `command`：下单、撤单、冻结、解冻、入账。
- `event`：OrderAccepted、TradeCreated、BalanceChanged、MarketDataUpdated。
- `control`：风控允许/拒绝、熔断、只减仓、维护模式。
- `evidence`：审计日志、幂等键、关联 ID、规则版本。

UI 应为不同通道提供稳定颜色和图例，不能只靠线型猜测。

---

## 五、黄金场景：BTC/USDT 市价买单

建议将其作为默认可运行测试向量，也是 UI、校验、追踪和 `.pip` 往返的共同验收场景。

### 前置条件

- 用户已通过 KYC，账户为 ACTIVE。
- MFA/交易密码策略满足要求。
- 用户不在制裁、冻结或地区限制名单。
- `BTC/USDT` 为 TRADING 状态。
- USDT 可用余额足够覆盖预估成交额和手续费。
- 市场数据未过期，撮合引擎处于可用状态。
- `clientOrderId` 在账户维度唯一。

### 主序列

1. API 接收 `PlaceOrderCommand`。
2. 生成或验证 `requestId / clientOrderId / correlationId`。
3. 校验账户、市场、数量、精度、价格保护和频率限制。
4. 计算最大可成交数量和费用上界。
5. 在账本侧幂等冻结 USDT。
6. 创建订单并进入 `ACCEPTED`。
7. 投递撮合命令，撮合引擎按价格优先、时间优先执行。
8. 生成一到多笔不可变 `Trade`。
9. 清算每笔成交：扣减冻结、增加 BTC、扣除费用、处理剩余冻结。
10. 更新订单为 `PARTIALLY_FILLED` 或 `FILLED`。
11. 发布订单、成交、余额和行情事件。
12. 审计节点关联命令、规则版本、账本分录和事件偏移量。
13. API/WS 向用户返回确定性最终状态或可查询的处理中状态。

### 异常分支

| 异常                 | 预期结果                                    |
| -------------------- | ------------------------------------------- |
| 余额不足             | 下单拒绝，不创建冻结和订单                  |
| 精度/最小金额错误    | 参数拒绝，返回规则版本                      |
| 重复 `clientOrderId` | 返回第一次请求结果，不重复冻结              |
| 市场暂停/熔断        | 订单拒绝或按规则取消未成交部分              |
| 部分成交后撤单       | 已成交部分清算，剩余冻结释放                |
| 撮合超时             | 保持可查询中间态，不盲目重试生成重复订单    |
| 清算失败             | 成交进入待修复队列，禁止静默丢失            |
| 消息重复             | 消费者以业务键幂等处理                      |
| 事件乱序             | 以市场序列号/账户序列号拒绝旧状态覆盖新状态 |

---

## 六、必须显式建模的业务不变量

这些约束不能只写在描述文本中，应成为可校验节点或 `invariants` 输出。

### 资金与账本

1. `total = available + frozen`，任何状态都不能出现负余额。
2. 每次资金变化必须对应平衡的双分录账本。
3. 同一冻结只能结算或释放一次。
4. 资产精度使用定点十进制或整数最小单位，禁止浮点数。
5. 成交前后资产守恒，差额只能来自明确的手续费或链上费用。

### 订单与成交

1. 订单状态只能按定义的有向状态机迁移。
2. `filledQuantity ≤ originalQuantity`。
3. Trade 不可变；修正必须使用补偿记录。
4. 每个市场的撮合序列严格单调。
5. `clientOrderId` 在约定作用域内幂等。
6. 撤单与成交竞争时，以撮合序列决定最终结果。

### 风控与合规

1. 使用的规则必须记录版本和生效时间。
2. 风控拒绝需要可解释原因码。
3. KYC、地区、制裁和账户状态变化必须能即时阻断新命令。
4. 大额、异常频率、可疑地址和资金路径需要进入审计管道。

### 可观测与恢复

1. 命令、订单、成交、账本和事件共享 `correlationId`。
2. 投递采用至少一次语义时，消费者必须幂等。
3. 状态可由快照 + 事件序列重建。
4. 交易 API 超时不等于业务失败，必须支持按幂等键查询结果。

---

## 七、工作台 Surface 优化建议

### 默认三 Surface 继续成立

```text
左上：意图结构树
左下：属性编辑器（默认只显示名称）
右侧：当前容器渲染器
```

针对交易所建模，建议提供两个可“另存为 View”的推荐组合：

### 1. 场景设计 View

- 意图结构树
- 当前容器渲染器
- 属性编辑器
- 静态校验

用途：设计节点、端口、绑定和不变量。

### 2. 运行审计 View

- 当前容器渲染器
- 运行追踪
- 静态校验
- 事件时钟/命令处理器

用途：观察一笔订单如何穿过管道，定位失败、重复和补偿。

### Surface 体验问题

1. **属性 Surface 位置记忆**：最小化应回到左下角，而不是缩到展开位置的左上角。
2. **新增 Surface 防重叠**：默认落点需避开现有 Surface。
3. **局部适应**：核心容器只有少量子节点时，适应视图应按内容包围盒居中，避免大片留白。
4. **管道降噪**：选中某交易步骤时，只强调它的上下游；其他跨 Surface 管道降低透明度。
5. **端口类型提示**：圆点 hover 显示通道、数据类型、幂等键和映射来源。
6. **上下文可见**：固定来源/固定节点应显示明确徽标，避免误以为 Surface 在跟随活动容器。

---

## 八、静态校验应增加的交易所规则

现有通用校验可继续保留，并增加领域规则包：

### 错误

- 容器输出未映射。
- 必填输入未绑定。
- 订单接受路径没有资金冻结。
- 成交输出没有进入清算。
- 清算没有生成账本分录。
- 账本分录不平衡。
- 使用浮点类型表达金额或数量。
- 状态机存在非法逆向迁移。

### 警告

- 风控规则没有版本。
- 命令没有幂等键。
- 事件没有序列号或关联 ID。
- 订单存在异常分支但没有补偿路径。
- 市场数据输出没有消费方。
- 审计证据没有覆盖资金变化。

### 提示

- 可增加部分成交 + 撤单竞争测试。
- 可增加撮合/清算超时恢复测试。
- 可增加充值确认数和链重组测试。
- 可增加费率变化的生效边界测试。

校验结果应能点击定位到对应节点、端口或管道，而不是只显示文本。

---

## 九、运行追踪建议

默认追踪一笔订单时，每一行至少展示：

| 字段      | 示例                              |
| --------- | --------------------------------- |
| 时间/序号 | `1042 / 12:10:01.235`             |
| 节点      | `balance_reservation`             |
| 输入摘要  | `accountId, symbol, quoteAmount`  |
| 输出摘要  | `reservationId, frozen=2500 USDT` |
| 状态      | SUCCESS / REJECTED / RETRYING     |
| 幂等键    | `clientOrderId`                   |
| 关联 ID   | `correlationId`                   |
| 规则版本  | `risk-rule-2026-07-01`            |
| 耗时      | `8 ms`                            |

建议增加：

- 按订单 ID、账户 ID、成交 ID和关联 ID过滤。
- 展开查看输入/输出 JSON 和账本分录。
- “仅看异常”“仅看资金变化”“仅看控制管道”。
- 从 Trace 行反向定位内树节点和画布管道。
- 对同一幂等键的多次请求做并排对比。

---

## 十、分步实施与回退

### 阶段 1：替换占位内容，不改变模型协议

- 保留默认样例既有主路径 ID。
- 将业务根、核心节点和参与者节点文案替换为交易所语义。
- 为 `scenario_flow` 增加 `stakeholders / market_catalog / fee_policy` 输入。
- 补齐现有输入消费、参与者输出消费和容器输出映射。

验收：既有样例路径和节点 ID 稳定；核心作用域校验达到 0 错误、0 警告；递归模型仍允许继续增加第五层及更深节点。

### 阶段 2：扩展第三层能力域

- 增加账户、资金、订单、撮合、清算、行情、风控审计节点。
- 第一轮先为各能力域增加一个可执行步骤，后续可继续递归细分，不把该步骤强制定义为永久叶节点。
- 建立数据、命令、事件、控制和证据管道。

验收：画布可读；主路径从准入到审计闭环；不存在孤立节点。

### 阶段 3：黄金场景可运行

- 固定 BTC/USDT 市价买单输入向量。
- 输出订单、成交、余额、账本、行情和审计结果。
- 加入余额不足、重复请求、部分成交撤单三个异常向量。

验收：相同输入与幂等键产生语义等价结果；Trace 可定位每一步。

### 阶段 4：领域校验与工作台体验

- 增加交易所不变量校验。
- 修复最小化位置记忆和新增 Surface 重叠。
- 增加场景设计 View、运行审计 View。

验收：校验问题可点击定位；两个 View 投射同一内树且上下文互不污染。

### 回退

- 每阶段独立提交。
- 业务内容改造不修改 v3 文档协议。
- 新增领域规则以注册表方式接入，可单独关闭或回退。
- 保留旧样例 `.pip` 作为兼容测试，不作为默认业务内容。

---

## 十一、验收清单

- [ ] 默认业务根名称为“数字货币现货交易所”。
- [ ] `scenario_flow → scenario_participants → scenario_actor_leaf` ID 与层级保持不变。
- [ ] 默认样例的既有四层路径保持稳定，同时任意复合节点可以继续下探到第五层及更深层。
- [ ] 核心节点所有环境输入至少有一个合法消费者。
- [ ] 核心节点所有输出均有内部映射。
- [ ] 不存在未消费的关键业务输出。
- [ ] BTC/USDT 市价买单主序列可完整运行。
- [ ] 余额不足不会生成订单或冻结。
- [ ] 重复 `clientOrderId` 不会重复冻结或重复成交。
- [ ] 部分成交后撤单能正确清算并释放剩余冻结。
- [ ] 金额和数量不使用浮点数。
- [ ] 账本始终平衡，余额不为负。
- [ ] 每个命令、成交、分录和事件可通过关联 ID 追踪。
- [ ] 工作台和自由布局引用同一棵交易所内树。
- [ ] 不同 Panel 的选择、导航和相机保持独立。
- [ ] 属性编辑器展开/最小化后回到预期左侧位置。
- [ ] 新增 Surface 不与现有三 Surface 默认重叠。
- [ ] `.pip` 往返恢复完整交易场景、View、Panel、Surface 和投射状态。

---

## 十二、推荐优先级

### P0：先让默认样例形成闭环

1. 替换占位文案。
2. 消费产品目标、业务约束、协作角色。
3. 映射 `scenario_flow` 输出。
4. 消除默认核心作用域的 1 警告 + 3 提示。

### P1：形成最小现货交易主链

1. 账户准入。
2. 余额冻结。
3. 订单生命周期。
4. 撮合成交。
5. 清算账本。
6. 风控审计。

### P2：让管道可观测、可验证

1. 交易所不变量校验。
2. 运行追踪字段增强。
3. 命令/事件/控制/证据通道图例。
4. 黄金场景与异常场景回归。

### P3：完善工作台体验

1. Surface 空闲落位。
2. 最小化位置记忆。
3. 场景设计与运行审计 View。
4. 跨 Surface 管道降噪和点击定位。

> 推荐判断：先不要直接扩展到合约、杠杆、法币 OTC、理财或链上 DeFi。现货交易的订单—撮合—清算—账本闭环本身就是验证“一切皆管道”最合适的最小业务内核；这个内核稳定后，其他业务可以作为新的场景子树或独立 `.pip` 包演进。

---

## 十三、应用 UI 实际填充记录（2026-07-27）

本节不是设计推演，而是通过应用 UI 在工作台和自由布局之间真实操作得到的结果。

### 已通过 UI 写入的业务内容

`scenario_flow` 已修改为：

```text
数字资产交易所核心业务流动场景与约束
├─ 参与方、账户准入与安全
│  └─ 执行并校验领域业务步骤
├─ 资产充值、提现与可用余额
│  └─ 执行并校验领域业务步骤
├─ 现货订单校验、资金冻结与生命周期
│  └─ 执行并校验领域业务步骤
├─ 价格时间优先撮合与成交生成
│  └─ 执行并校验领域业务步骤
└─ 成交清算、双分录账本、行情与风控审计
   └─ 执行并校验领域业务步骤
```

核心容器描述已写入用户准入、资产充值、下单、冻结、撮合、清算、账本、行情、提现、风控和审计约束；核心输出已从“未映射”改为映射现有参与者结果。

静态校验实测：

```text
0 错误 · 0 警告 · 6 提示
- 产品目标未被消费
- 业务约束未被消费
- 资产充值、提现与可用余额输出未被消费
- 现货订单校验、资金冻结与生命周期输出未被消费
- 价格时间优先撮合与成交生成输出未被消费
- 成交清算、双分录账本、行情与风控审计输出未被消费
```

### 填充过程中发现的 P0 问题

#### 1. 功能 Surface 的命令使用了错误 Panel 上下文

- 工作台当前容器已下探到 `scenario_flow`，但新开的“当前作用域工具条”仍显示自由布局的 `business_root`。
- 工作台属性编辑器正在显示“参与方、账户准入与安全”，点击“创建副本”却复制了自由布局中选中的 `scenario_flow`。
- 必须先切到自由布局选中正确节点，再返回工作台新建一个属性 Surface，复制命令才会作用到目标节点。

**建议**：所有 `NodeImplementationContent` 命令必须只消费传入的 `{ panelId, surfaceId, contextSource, subject }`，禁止读取自由布局的全局 `selectedBusinessNodeId / businessScope`。

#### 2. “创建副本”会让子节点继续共享同一 ID

- 复制四个复合节点后，树中出现五个同名第四层叶节点。
- 修改其中一个叶节点，五处名称和描述同时变化。
- UI 没有显示它们是链接实例，用户会以为是五个独立副本。

**建议**：

1. 将动作拆成“深复制”“创建链接实例”两个明确命令。
2. 深复制必须为整棵子树生成新 ID，并重写内部 binding。
3. 链接实例必须显示链接标记、源节点和“编辑将同步到 N 个实例”的提示。
4. 操作完成后显示 toast，并高亮新节点。

#### 3. v3 导出在真实 UI 中不可用

以下路径均未产生下载，也没有从“未导出”切换为“已导出”：

- 工作台“顶栏与全局命令”Surface → 导出 v3。
- 自由布局应用根的真实“顶栏与全局命令”节点 → 导出 v3。
- `Ctrl+S`。
- 按钮聚焦后按 Enter。

**影响**：当前填充结果只能保留在活动浏览器标签页内，无法形成可导入的持久化文件。

**建议**：导出命令增加成功/失败结果事件；成功必须返回文件名、字节数和 SHA-256，失败必须显示具体原因。为 blob 下载增加浏览器回归测试。

### 填充过程中发现的 P1 问题

#### 4. 非空容器没有“添加子意图”入口

- 当前容器只有在 `children.length === 0` 时显示“＋ 添加子意图”。
- `scenario_flow` 已有一个子节点后，默认三 Surface 内没有新增同级节点的入口。
- 作用域工具条虽然有“＋ 子意图”，但因上下文错配会写到业务根。

**建议**：当前容器标题栏和画布空白区始终提供“添加子意图”，并明确目标父节点名称。

#### 5. UI 无法编辑端口模式

- 属性编辑器只能修改已有 input binding 和 output binding。
- 无法新增、删除、重命名端口，也无法修改类型和通道。
- 复制得到的交易节点仍全部输出“参与者模型”，且没有输入端口，因此无法用拖线形成订单—撮合—清算管道。

**建议**：属性编辑器增加端口 Schema 编辑，包括名称、类型、必填、通道、幂等键、默认值和映射；修改前显示受影响管道。

#### 6. Surface 操作在重叠时失效

- 属性编辑器从左侧展开后再最小化会移动到左上角，覆盖节点树。
- 被覆盖的最小化 Surface 无法正常获得焦点和展开。
- 新增校验/命令 Surface 默认重叠；其关闭和导出按钮有时可定位但不产生状态变化。

**建议**：增加 `minimizedFrame`；Surface pointer-down capture 必须先提升 z-index；新增 Surface 使用空闲矩形布局；所有命令按钮在被遮挡时仍需满足可操作性检测。

### 当前结果的边界

- 核心交易业务域和约束文字已经通过 UI 写入。
- 本次 UI 填充没有改变默认样例现有深度，但这只是当前编辑结果，不是模型上限。
- 因端口 Schema 不可编辑，阶段之间尚未形成真实数据管道。
- 因子节点共享 ID，第四层只能使用通用执行契约，不能分别写入五种阶段实现。
- 因导出失效，结果需依赖当前活动浏览器标签页继续检查。
