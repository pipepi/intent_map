# Intent Map 逐层逐功能优化建议

> 走查方式：Playwright 无头浏览器对 `http://localhost:7100/` 实测（截图存于 `.review/shots/`），结合源码核对。
> 理念标尺：**一切皆管道（节点）**——节点承载功能，管道（边）承载关系，两者都应是一等公民。

---

## 一、严重缺陷（阻塞基本使用，建议最高优先级）

### 1. 鼠标点击被画布平移手势吞噬 —— 面板控件形同虚设
- **现象**：进入任意叶子节点（顶栏与全局命令、属性编辑器、运行追踪等）后，面板里的按钮/下拉用鼠标点击**完全无效**；业务画布叶子层的「＋ 添加子意图」同样无效。JS 触发（无障碍/键盘路径）则全部正常。
- **根因**：`app/page.tsx` 画布视口的 `onPointerDown` 平移处理器会调用 `setPointerCapture`（L1080），而渲染功能面板的 `<section className="focused-runtime-content">`（L2689）**没有 stopPropagation**。指针被捕获后 `click` 事件重定向到视口，按钮永远收不到点击。实测控制台报错 `NotFoundError: setPointerCapture`。
- **建议**：
  1. 功能面板容器统一加 `onPointerDown={(e) => e.stopPropagation()}`；
  2. 更彻底：平移手势只在命中空白画布时启动（`event.target === event.currentTarget` 或专用背景层），让"内容优先、手势让路"成为全局原则——这本身就符合「节点优先」的理念。
- **影响面**：工具条全部命令、属性编辑器、运行追踪、意图树、模块库、业务画布添加子意图。**此前我以为"运行/导出坏了"，实际全是这一个问题。**

### 2. 「＋ 添加子节点」悬浮按钮遮挡工具条命令
- **现象**：进入「顶栏与全局命令」后，悬浮的「＋ 添加子节点」按钮正好压在「兼容 v1 / 撤销 / 重做」三个按钮上（`elementFromPoint` 实测确认遮挡），这些按钮无法点击。
- **建议**：把「添加子节点」收进作用域工具条，或固定到画布右下角等不遮挡内容的位置；悬浮按钮应做碰撞检测。

### 3. 「新建」无确认直接重置整个文档
- **现象**：点击「新建」立即替换文档，无确认对话框。虽有撤销兜底，但导入的外部文档或未导出修改容易误损。
- **建议**：新建前若 `dirty` 则弹确认，或提供「新建并自动导出当前」选项。

---

## 二、层级与导航（分形下钻体验）

### 4. 业务深层面包屑语义混乱
- **现象**：下钻到第 2~4 层时，面包屑末位恒显示「当前业务容器」，真实节点名被插在中间，如 `应用根 › 当前容器渲染器 › Agentic 软件开发框架 › 核心物流动场景序列与约束 › 当前业务容器`（当前页其实是"核心物流动场景序列与约束"）。
- **建议**：面包屑直接显示业务节点链，末位 = 当前节点名；隐藏「当前容器渲染器/当前业务容器」这类实现层名词。

### 5. 进入业务画布需要三次跳转，「解引用」一跳内容单薄
- **路径**：应用根 →（双击×2）当前容器渲染器 →（双击）当前业务容器引用 → 业务画布。中间一跳只有一个引用节点占位。
- **建议**：双击「当前容器渲染器」时直接解引用进入业务画布；或让引用节点默认展开为实时投影（它本来就是 always-live）。

### 6. 手势不可发现
- 展开节点 = 双击一次，进入节点 = 再双击一次；Ctrl+滚轮放大到底也会进入节点。这些都没有任何界面提示，新用户必然卡在「为什么双击没反应/为什么突然进去了」。
- **建议**：节点 hover 时显示操作提示（双击展开 / 再双击进入）；提供右键菜单与键盘 Enter 进入；在「画布状态」面板已有的手势图例基础上，首屏加一次性引导浮层。

### 7. LOD 摘要态容易误读
- 缩放 < 0.55 时，已展开节点只显示描述文字（`node-renderer.tsx` L75），看起来像"展开了但内容是空的"。
- **建议**：摘要态追加一行「放大至 75% 以上查看交互面板」，或按节点实际屏幕面积而非全局缩放决定 LOD。

### 8. 键盘流断裂
- 现有快捷键仅 Esc（返回）/ Home（适应）/ 0（100%）。**没有 Ctrl+Z / Ctrl+Y**，撤销重做必须进入工具条节点点按钮（还正被缺陷 #2 遮挡）。
- **建议**：补齐 Ctrl+Z/Y/S（导出）、方向键选择移动、Delete 删除选中；无小地图/总览视图，大图导航成本高，建议加 minimap。

---

## 三、第一层：应用根画布

### 9. 连线交叉严重、不可读
- 应用状态/文档加载器各发出多条边，互相穿越成"毛线团"（见 `shots/01-app-root.png`）。这与「一切皆管道」的展示初衷相悖——管道应是可读的一等公民。
- **建议**：正交/曼哈顿时路由、边捆绑（edge bundling）、hover 节点时只高亮其相连管道、默认按 runtime/interface/output 三泳道分层布局（现有「泳道布局」按钮效果不明显，建议设为默认并验证）。

### 10. 首屏信息密度与可读性
- 默认全部最小化 + 适配缩放，节点名几乎不可读；用户第一眼不知道能干什么。
- **建议**：首屏默认聚焦 interface 泳道（约 100% 缩放），或默认展开「顶栏与全局命令 + 当前容器渲染器」两个核心节点。

### 11. 统计口径与图例
- 「6 管道」在进入业务层后变「0 管道」，统计口径随作用域切换但无说明；核心节点 ◆ 标记无图例。
- **建议**：管道数注明口径（本作用域/全局）；◆ 加入画布状态图例。

---

## 四、第二~四层：业务画布

### 12. 适配视图偏上、大片留白
- 业务内容集中在画布上半部，fit 后缩放 104% 且偏顶，下半屏全空（见 `shots/25-business-canvas.png`）。
- **建议**：fitScope 按内容包围盒而非 canvasSize 计算；业务画布默认收紧 canvasSize。

### 13. 静态校验只有"循环依赖"一项
- 环境输入「协作角色」没有任何出边（无人消费），校验面板仍显示 ✓ 作用域有效。未使用端口、未绑定输入、类型不匹配都不报。
- **建议**：校验升级为分级问题列表（错误/警告/提示）：未绑定输入、孤立节点、未消费输出、类型不匹配、循环依赖。

### 14. 不能在画布上直接连线 —— 与理念最核心的一处落差
- 管道（绑定）目前只能在属性编辑器里用下拉框选择来源。「一切皆管道」，但管道的创建/删除在画布上不可见、不可操作。
- **建议**：支持从输出端口拖拽到输入端口创建绑定、点击边删除/改绑；边 hover 显示承载的端口映射明细（现在聚合边只有数量）。

### 15. 运行追踪不展示数据
- 运行成功后有 7 步分层追踪（路径、状态、耗时都对），但每步的实际输入/输出值（`Trace.output` 已记录）界面不展示。
- **建议**：trace 行可展开查看输入/输出 JSON；这样"管道里流的是什么"才可见——管道可观测性的关键一环。

### 16. 根输入编辑器无类型意识
- 运行追踪里「协作角色」端口类型是 array，但给的输入框是纯文本，填进去的是字符串。
- **建议**：按端口类型给对应编辑器（string→文本、object/array→JSON 编辑器、boolean→开关），提交前做类型校验提示。

---

## 五、功能面板逐项

| 面板 | 实测结果 | 建议 |
|---|---|---|
| 顶栏与全局命令 | JS 触发下全部可用；导出 v2/v1 产物正确 | 修缺陷 #1/#2；「兼容 v1」改名「导出 v1（兼容）」更直白 |
| 意图结构树 | 搜索过滤正常；副本/链接实例同步显示 | 节点计数徽标（6/1/0）含义不明，加 title 说明；点击树节点建议画布同步高亮居中 |
| 模块库 | 发布 v1、插入链接实例全通 | 插入后无反馈（无 toast、画布无高亮）；模块缺删除/更新/版本对比 |
| 静态校验 | 仅循环依赖 | 见 #13 |
| 属性编辑器 | 改名、改绑定、创建副本、删除均可用 | 无保存反馈；绑定下拉把"环境变量"和"节点输出"混排，建议分组；算子表达式（concat/add/and/or）无编辑 UI |
| 运行追踪 | 状态机、重跑、根输入正常 | 见 #15/#16 |
| 意图执行器 | 执行业务根 → SUCCESS，7 步 | 「停止」按钮因运行过快永远看不到，属正常；可加人工延时/单步调试模式（契合确定性管道理念） |
| 事件时钟 / 命令处理器 | tick/队列/最近事件正常 | mini trace 只显示 4~5 条，建议展开为完整事件日志（管道可观测性） |
| 面包屑导航 | 仅显示业务根一层 | 见 #4 |
| 当前作用域工具条 | 功能最全（上级/锁定/布局/复制/删除/重置/适应/子意图） | 建议把它的一部分能力上浮为常驻 UI，而不是藏在节点里 |
| 文档加载器 / 应用状态 / 画布状态 | 正常 | 画布状态的手势图例建议首屏可见（见 #6） |

---

## 六、文案与理念一致性

### 17. 品牌文案与理念不符
- 顶栏品牌写着「**一切皆节点** · v2」，而项目理念是「**一切皆管道（节点）**」。
- **建议**：改为「一切皆管道 · 节点即世界」或「一切皆管道（节点）」。

### 18. 术语混用
- 意图 / 节点 / 容器 / 作用域 / 管道 / 引用 六套词并存（如「添加子意图」vs「添加子节点」两个按钮并存且语义不同）。
- **建议**：统一术语表：节点（Node）、管道（Pipe/Edge）、容器（Composite）、作用域（Scope），UI 文案全部对齐；「＋添加子节点」「＋添加子意图」统一命名并说明差异。

---

## 附：走查资产

- 截图：`shots/01-app-root.png`（首层全景）、`shots/10-toolbar-scope.png`（工具条）、`shots/11-toolbar-running.png`（遮挡实证）、`shots/20-business-root.png`（引用节点）、`shots/25-business-canvas.png`（业务画布）、`shots/30~33`（逐层下钻）、`shots/41-runtrace-filled.png`（运行追踪）、`shots/42~45`（属性编辑）、`shots/47-module-library.png`（模块库）、`shots/48~56`（各面板）
- 走查脚本：`s1-survey.cjs` ~ `s13-export.cjs`（可重复执行回归）
- 验证环境：Playwright Chromium（装于 `.review/`，与项目依赖隔离）

> 注：本次因浏览器 WebBridge 扩展未连接，改用无头浏览器走查；功能面板的鼠标点击问题（缺陷 #1）正是通过这种"真实鼠标 vs JS 触发"对照发现的。如需在真实浏览器里边看边讨论，装好扩展后我可以再走一遍。

---

## 实施记录（2026-07-25，按报告推荐选项执行）

全部改动通过 `tsc --noEmit`（0 错误）与 Playwright 无头回归（`.review/s14` ~ `s21`）。预览：http://localhost:7100/

### 第一批 · 严重缺陷
1. **平移守卫吞点击**：`onViewportPointerDown` 对按钮/输入/端口等交互元素直接 return，不再 `setPointerCapture`——修复节点上按钮、端口、面板无法点击的问题。
2. **`＋添加子意图` 按钮遮挡**：`.runtime-add-child` 从底部中央移至右下角（`left:auto; right:36px` + `transform:none !important`），不再压住节点内容。
3. **新建文档防误丢**：有未保存修改时 `window.confirm` 确认后才新建。

### 第二批 · 速赢与导航
4. 品牌标语更新为「一切皆管道（节点）· v2」。
5. **快捷键**（`actionRefs` ref 驱动，输入框内自动豁免）：Ctrl+Z/Y 撤销重做、Ctrl+S 导出、Enter 进入选中节点、Delete 删除选中。
6. **导航减层**：`enterNode` 遇 current_container 直进业务画布；`navigateToBusinessNode` 跳过容器层；面包屑末位显示当前节点名。
7. 管道图例补充说明；节点渲染器增加悬停与 LOD 提示。

### 第三批 · 中期体验
8. **运行追踪可读化**：trace-row 内联展示输出/错误 JSON。
9. **根输入按类型渲染**（text/textarea），`run()` 宽松 JSON 解析（仅 `{`/`[` 开头才 parse），非法 JSON 给出具体 toast。
10. **三级校验**：错误/警告/提示分级展示（`.validation-issue-list`）。
11. **适应画布（Home）**：按业务内容包围盒计算相机，留 200px 边界。
12. **拖拽连线**：输出端口拖至输入端口创建管道（虚线临时连线 + crosshair 光标 + 端口 `pointer-events:auto`），双击输入端口断开绑定。实测边数 5→6→5，toast 正确。

### 第四批 · 收尾（本轮）
13. **边高亮**：选中节点时相连边加粗发光（`edge-connected`），其余减淡（`edge-dim`），应用画布与业务画布均生效。实测：业务 6 边 = 2 高亮 + 4 减淡；应用 20 边 = 2 高亮 + 18 减淡。
14. **首屏密度**：`global_toolbar`、`intent_tree` 与 `current_container` 默认展开（`app/runtime/model.ts`），其余保持最小化。
15. **撤销/视图分离**：新增 `commitView`/`updateDocumentNodeView`（不入撤销历史），四个展开/缩放切换改走视图提交；同时修复**单击节点也推历史**的隐藏缺陷——4 个 drag/resize 处理器（应用+业务）在 pointerup 时仅当位移 > 3px 才提交。实测：展开后 Ctrl+Z 不再回退展开；改名后 Ctrl+Z 正常还原。

### 回归证据
- `s21-final.cjs`：首屏默认展开 3 项 ✓、视图操作免撤销 ✓、改名撤销还原 ✓、业务/应用边高亮计数 ✓
- 截图：`shots/90-first-screen.png`（首屏）、`shots/91-biz-edge-highlight.png`（业务边高亮）、`shots/92-app-edge-highlight.png`（应用边高亮）
