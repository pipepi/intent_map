# Software Authoring

The default business-independent a3 extension. It interprets software intent
nodes and will own projections, controlled build adapters, tests, previews and
promotion. a2 only preserves its opaque node payloads.
